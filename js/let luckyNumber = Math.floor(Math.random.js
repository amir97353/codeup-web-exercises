let luckyNumber = Math.floor(Math.random() * 6)

let price = prompt("How much is the orginal price")
let number = prompt("How much is the discount?")
function calculateTotal(price,discount) {
  
  
  
  let luckyNumber = Math.floor(Math.random() * 6)
  
  
  
  if(luckyNumber === 0 ) {
    let total = price - discountPrice
    let discountPrice = price * discount
    let discount = 0.25
    price * discount
    price - discountPrice
    console.log(total)
  } 
}




let discount = 0.25
    price * discount
    let discountPrice = price * discount
    price - discountPrice
    let total = price - discountPrice
    console.log(total)